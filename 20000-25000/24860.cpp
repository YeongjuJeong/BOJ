// 24860 Counting Antibodies

#include <iostream>
using namespace std;

int main() {
    long long a, b, c, d, e, f, g;
    cin >> a >> b >> c >> d >> e >> f >> g;
    cout << (a * b + c * d) * e * f * g;

    return 0;
}
